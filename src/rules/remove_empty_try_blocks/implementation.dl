/* Pre  try {} catch ([:exception]) {} */
/* Post                                */
rewrite("remove_empty_try_blocks", filename, start, end, "") :-
    try_statement(id, body, _, nil),
        filename_of(id, filename),
        starts_at(id, start),
        ends_at(id, end),
    block(body, nil).
