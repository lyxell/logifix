/* Pre  try {} catch ([:exception]) {} */
/* Post                                */
rewrite(10001, filename, start, end, "") :-
    try_statement(id, body, _, nil),
        filename_of(id, filename),
        starts_at(id, start),
        ends_at(id, end),
    block(body, nil).
