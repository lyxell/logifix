/* Pre  if ([:x]) {} */
/* Post              */
rewrite("EmptyIfStmt", filename, start, end, "") :-
    if_statement(id, _, body, nil),
    block(body, nil),
        filename_of(id, filename),
        starts_at(id, start),
        ends_at(id, end).
