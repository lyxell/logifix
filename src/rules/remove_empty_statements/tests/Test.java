import java.util.ArrayList;

public class Test {
    public void x(boolean y) {

        ArrayList<Integer> list = new ArrayList<>();
        list.add(0);

        // remove
        ;

        while (y) {
            // remove
            ;
        }

        // keep
        if (y);

        // keep
        for (int i = 0; i < 10; i++);

        // keep
        for (Integer x : list) ;

        // keep
        while (y) ;

        // keep
        do ; while (y);

        // keep
        if (true) {} else ;

    }
}
