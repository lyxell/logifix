/**
 * Empty statements should be removed
 * https://rules.sonarsource.com/java/RSPEC-1116
 */

rewrite(1116, filename, start, end, "") :-
    empty_statement(id),
    ! for_statement(_, _, _, _, id),
    filename_of(id, filename),
    starts_at(id, start),
    ends_at(id, end).
