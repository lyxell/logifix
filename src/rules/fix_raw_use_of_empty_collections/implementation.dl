rewrite_node("fix_raw_use_of_empty_collections", id, "Collections.emptyList()") :-
    string_representation(id, "Collections.EMPTY_LIST").
rewrite_node("fix_raw_use_of_empty_collections", id, "Collections.emptyMap()") :-
    string_representation(id, "Collections.EMPTY_MAP").
rewrite_node("fix_raw_use_of_empty_collections", id, "Collections.emptySet()") :-
    string_representation(id, "Collections.EMPTY_SET").
