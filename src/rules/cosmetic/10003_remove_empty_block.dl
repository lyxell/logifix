/* Pre  {} */
/* Post    */
rewrite(10003, filename, start, end, "") :-
    block(_, stmts),
    list_contains(stmts, id),
    block(id, nil),
        filename_of(id, filename),
        starts_at(id, start),
        ends_at(id, end).
