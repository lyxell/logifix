rewrite_node("simplify_calls_to_map_keyset", id, "Collections.emptySet()") :-
    method_invocation(id, subject, "keySet", nil),
    method_invocation(subject, collections, "emptyMap", nil),
    string_representation(collections, "Collections").
