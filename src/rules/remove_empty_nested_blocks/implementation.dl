/* Pre  {} */
/* Post    */
rewrite("remove_empty_nested_blocks", filename, start, end, "") :-
    block(_, stmts),
    list_contains(stmts, id),
    block(id, nil),
        filename_of(id, filename),
        starts_at(id, start),
        ends_at(id, end).
